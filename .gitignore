# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (contain secrets) — keep the example template tracked
.env
.env.*
!.env.example
# Build-time public config for VM deploys (anon key is public/RLS-protected)
!.env.production

# Build/tool caches
.vite

# FUSE filesystem leftovers (orphaned open-file copies)
.fuse_hidden*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
